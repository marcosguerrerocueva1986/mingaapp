name: bancamovilr
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 3.0.0+11

environment:
  sdk: ">=3.3.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  dio: ^5.4.0
  dropdown_search: ^6.0.1
  calendar_date_picker2: ^2.0.1
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^10.0.0
  darq: ^2.0.0
  date_format: ^2.0.7
  easy_debounce: ^2.0.3
  encrypt: ^5.0.3
  flutter:
    sdk: flutter
  pointycastle: ^3.9.1
  excel: ^4.0.6
  archive: ^3.6.1
  basic_utils: ^5.7.0
  flutter_riverpod: ^2.5.1      
  flutter_screenutil: ^5.9.0
  flutter_svg: ^2.0.9
  collection: ^1.18.0
  freezed_annotation: ^2.4.4    
  get_it: ^9.2.1
  get_storage: ^2.1.1
  google_fonts: ^6.2.1
  google_maps_flutter: ^2.10.0
  grouped_list: ^6.0.0
  intl: ^0.20.2
  json_annotation: ^4.9.0       
  line_icons: ^2.0.3
  maps_launcher: ^3.0.0
  no_screenshot: ^0.9.2
  once: ^1.4.0
  permission_handler: ^12.0.1
  pin_code_fields: ^8.0.1
  pretty_dio_logger: ^1.3.1
  camera: ^0.11.0
  google_mlkit_barcode_scanning: ^0.14.1
  quickalert: ^1.0.2
  reactive_forms: ^17.0.0
  retrofit: ^4.1.0
  auto_route: ^10.1.0+1             
  riverpod_annotation: ^2.3.5    
  share_plus: ^12.0.1
  sks_ticket_view: ^1.0.0
  ticket_widget: ^1.0.2
  widgets_to_image: ^2.0.1
  local_auth: ^2.1.8
  provider: ^6.1.2
  flutter_typeahead: ^5.2.0
  crypto: ^3.0.3
  pdf: ^3.10.8
  path_provider: ^2.1.2
  url_launcher: ^6.3.0
  flutter_widget_from_html: ^0.16.1
  syncfusion_flutter_pdfviewer: ^29.1.38

dev_dependencies:
  riverpod_generator: ^2.4.0    
  auto_route_generator: ^10.0.1  
  build_runner: ^2.4.9
  change_app_package_name: ^1.1.0
  envied_generator: ^1.1.1      
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.4.7               
  json_serializable: ^6.8.0      
  retrofit_generator: ^9.6.0     
  flutter_launcher_icons: ^0.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec
# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    # mek_assets GENERATED CODE - DO NOT MODIFY BY HAND
    - assets/documentos/
    - assets/images/imagenpromocion/
    - assets/images/error-deuna-qr.png
    - assets/images/error-deuna-transaccion.png
    - assets/images/icon.png
    - assets/images/logo-deuna-blanco.png
    - assets/images/logo-deuna-morado.png
    - assets/images/logopantallamenu.png
    - assets/images/imagenmenu.png
    - assets/images/marcaagua.png
    - assets/images/onboarding-deuna.png
    - assets/svg/logo-black.svg
    - assets/svg/logo.svg
    - assets/svg/logoPantallas.svg
    - assets/images/logo1.png
    - assets/images/logo2.png
    - assets/images/logomitad.png
    - assets/images/usuariocontrasenia.png
    - assets/images/accesobiometrico.png
    - assets/images/huelladactilar.png
    - assets/images/huelladactilar_icon.png
    - assets/images/pin.png
    - assets/images/activacuenta.png
    - assets/images/fondoSplash.jpg
    - assets/images/fondoLogin.jpg
    - assets/images/misproductos.png
    - assets/images/imagencardinformacion.jpg
    - assets/images/cloudstudiologo.jpg
    - assets/images/compartircuenta.png
    - assets/images/descubre.png
    - assets/images/ojocuenta.png
    - assets/images/transferirdinero.png
    - assets/images/nuestrasagencias.png
    - assets/images/pagarservicios.png
    - assets/images/deuna.png
    - assets/images/estadocuenta.png
    - assets/images/portalpagos.png
    - assets/images/bienvenidos.png
    - assets/images/verdetalles.png
    - assets/images/cargando.png
    - assets/svg/cargando.svg


    # mek_assets
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware
  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/images/icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21

mek_assets:
  format: tree
  groups:
    - input_dir: assets
      class_name: R
      create_map_files: true
      output_file_name: r

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/icon.png"
  min_sdk_android: 21 # android min sdk min:16, default 21
  web:
    generate: true
    image_path: "path/to/image.png"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: true
    image_path: "path/to/image.png"
    icon_size: 48 # min:48, max:256, default: 48
  macos:
    generate: true
    image_path: "path/to/image.png"
dependency_overrides:
  pointycastle: ^3.9.1